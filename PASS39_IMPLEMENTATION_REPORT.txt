PASS39 IMPLEMENTATION REPORT

Files changed:
- src/components/explore/ExploreListingCard.tsx
- src/pages/Explore.tsx
- src/pages/Billing.tsx
- src/pages/Settings.tsx

Issues fixed:
1. Marketplace cards still truncated category/location labels too aggressively.
2. Explore page did not surface enough high-level context (results, location, radius, mix) and was not RTL-aware.
3. Billing lacked provider/status filtering and per-connection sync visibility.
4. Settings page was not explicitly RTL-aware and lacked quick navigation between major settings blocks.

Fixes applied:
- ExploreListingCard:
  - made card container RTL-aware
  - added category breadcrumb (group · subcategory)
  - reduced hard truncation on badge/location text
- Explore:
  - made page RTL-aware
  - added results summary cards (results, location, radius, mix)
  - added localized location summary
- Billing:
  - added bank status/provider filters
  - added provider counts helper
  - added visibleBankConnections filtering
  - added localized date formatting helper
  - displayed per-connection last sync information
- Settings:
  - made page RTL-aware
  - added quick jump chips to wallet/preferences/notifications/orbit-security/sessions/branding sections

QA:
- npm run typecheck => PASS
