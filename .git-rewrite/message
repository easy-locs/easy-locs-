ACP Agent 3 (#863) — Legacy admin URL redirects → /admin/control/*

Implements task #863: every legacy admin agent URL now redirects into the
unified Agent 1 control shell (Agent 4+ will replace its UI). No 404, no white
screen, query strings preserved, :slug promoted to ?agent=:slug.

Changes (easy-locs-ea1eb0ed/src/routes/admin.routes.tsx)
- Added LegacyControlRedirect wrapper preserving location.search.
- Added LegacyAgentRunsRedirect promoting :slug → ?agent=:slug while keeping
  any pre-existing query params.
- Removed the 8 legacy <Route> declarations and re-registered them as Navigate
  redirects placed AFTER the /admin/control shell routes:
    /admin/agents              -> /admin/control/agents
    /admin/agents/:slug/runs   -> /admin/control/runs?agent=:slug
    /admin/command-center      -> /admin/control/command
    /admin/approvals           -> /admin/control/approvals
    /admin/autonomy            -> /admin/control/autonomy
    /admin/control-room        -> /admin/control/engines
    /admin/engine-control-room -> /admin/control/engines
    /admin/master-control      -> /admin/control/master
- All redirects use replace to avoid back-button loops.

Rebase resolution
- Conflict in admin.routes.tsx around the /admin/control shell block: upstream
  (HEAD) added an expanded shell comment and explicit per-section routes
  (/admin/control/agents, /runs, /command, /approvals, /master) wrapped in
  ProtectedRoute > SuperAdminGate, and kept /admin/command-center as a real
  SuperAdminGate route. Resolved by keeping all of upstream's new shell
  routes/comment and dropping upstream's /admin/command-center real route in
  favor of #863's redirect (the redirect now lands on the shell's gated
  /admin/control/command, which is functionally equivalent and matches the
  task's locked mapping). Added a note in the shell comment so the next agent
  understands why /admin/command-center is missing from the shell block.

Notes / scope adherence
- Per task scope ("Suppression des anciennes pages interdit"), the page
  components themselves remain exported from app-route-registry; only the
  route bindings were swapped. Destructured names left in place since
  noUnusedLocals is off.
- TypeScript build passed pre-rebase (npx tsc -p tsconfig.app.json --noEmit).
- No follow-ups proposed: downstream task "ACP Agent 10 — QA final, build,
  console, responsive" already covers verification.

Replit-Task-Id: 53249510-b7da-4ac9-ab68-a8512a96ed9e
