Task #927 — P3 (rides + marketplace) phase plan & drain gate

This task was scoped as a per-phase L7 drain (remove the 157
`owning_phase: "P3"` entries from the dispatch allow-list, behind
`agent.rides.enabled` / `agent.marketplace.enabled`). On code review the
mechanical drain was rejected as unsafe: removing the entries while the
underlying `.insert / .update / .delete / .upsert / .rpc` callers in those
157 files have not yet been migrated to `dispatchExecutionTask` would
weaken the L6 dispatch-guard contract rather than strengthen it (the
marketplace adapter bundle covers only the listing lifecycle today, and
no rides adapter bundle exists yet).

Deliverable, after rescope:
- A signed phase plan + drain-gate document at
  `docs/audits/agent-migration/p3-rides-marketplace.md` that fully
  inventories the P3 surface (62 rides/mobility/leasing files +
  95 marketplace ops files), records which adapters / verifiers / rollback
  contracts are already in place vs missing, defines the seven-checkbox
  drain gate (adapters, verifiers, rollback, feature flags, caller
  migration, governance SQL, cockpit visibility), and rehearses the
  mechanical drain procedure that runs once the gate is green.
- `docs/architecture/agent-migration-inventory.md` §3: P3 row updated to
  point at the new phase-plan doc (count remains 157 — the drain has not
  happened).
- `.eslintrc.dispatch-allowlist.json`: **unchanged from the #914
  baseline** — 456 per-file exemptions (157 P3 + 79 P1 + 38 P2 + 149 P4
  + 33 P5), `policy.last_audit` still references task #914. An earlier
  attempt this session removed the 157 P3 entries and updated
  `policy.last_audit` to reference #927, but that was reverted to match
  the reviewer's safety bar.

Drift vs. original task description:
- "Done looks like" demanded the rides + marketplace adapters in
  production with verifiers + rollback, every P3 mutation routed through
  `dispatchExecutionTask`, and the 157 entries removed. Only the
  governance / planning artifact landed in this task; the adapter
  authoring + 157 caller migrations are explicitly deferred to the work
  the new phase-plan doc gates.
- Entry count was 157, not 155 as the task description quoted (matches the
  `phase_distribution` block recorded by audits #908 and #914).

Follow-ups proposed:
- #937 — Verify rides + marketplace migration with a 24-hour governance
  audit. **CANCELLED** after rescope (no drain happened, so there is
  nothing to verify yet; that work is folded into the drain gate itself).
- #938 — Drain the P4 (content + contacts) entries from the dispatch
  allow-list. Still valid as the next per-phase L7 task.

Replit-Task-Id: 82b7f988-6ad7-4680-b19d-63995187fb51
