// Timeout settings and splash functionality.
const TTFR_BUDGET_MS = 2000; // Production timeout fix

setTimeout(fadeOutHtmlSplash, 1500); // Production timeout fix

// Removed skip button logic to prevent deadlock issues at startup

function fadeOutHtmlSplash() {
    // Your existing fade-out splash code
}
