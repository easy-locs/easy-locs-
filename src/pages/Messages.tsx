// Legacy Messages page — redirects to unified CommunicationCenter
import { Navigate } from "react-router-dom";

const Messages = () => <Navigate to="/dashboard/communication" replace />;

export default Messages;
