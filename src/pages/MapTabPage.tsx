/**
 * MapTabPage — Map tab entry point. Uses ExplorerMap as the full-screen experience.
 */
import ExplorerMap from "@/components/map/ExplorerMap";

export default function MapTabPage() {
  return <ExplorerMap />;
}
