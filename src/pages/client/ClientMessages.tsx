// Absorbed into Orbit — redirects to unified communication shell
import { Navigate } from "react-router-dom";

const ClientMessages = () => <Navigate to="/orbit" replace />;

export default ClientMessages;
