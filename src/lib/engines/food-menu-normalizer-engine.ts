/** Stub — engine logic moved to backend functions. */
export async function runFoodMenuNormalizer(..._args: any[]): Promise<any> {
  console.warn("[food-menu-normalizer-engine] Stub — no-op");
  return { status: "stub", results: [] };
}
