/** Stub — engine logic moved to backend functions. */
export async function runAdaptiveTaxonomy(..._args: any[]): Promise<any> {
  console.warn("[adaptive-taxonomy-engine] Stub — no-op");
  return { status: "stub", results: [] };
}
