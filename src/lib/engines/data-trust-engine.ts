/** Stub — engine logic moved to backend functions. */
export async function runDataTrust(..._args: any[]): Promise<any> {
  console.warn("[data-trust-engine] Stub — no-op");
  return { status: "stub", results: [] };
}
