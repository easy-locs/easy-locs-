/** Stub — engine logic moved to backend functions. */
export async function runGroceryNormalizer(..._args: any[]): Promise<any> {
  console.warn("[grocery-normalizer-engine] Stub — no-op");
  return { status: "stub", results: [] };
}
