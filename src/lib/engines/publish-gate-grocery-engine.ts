/** Stub — engine logic moved to backend functions. */
export async function runPublishGateGrocery(..._args: any[]): Promise<any> {
  console.warn("[publish-gate-grocery-engine] Stub — no-op");
  return { status: "stub", results: [] };
}
