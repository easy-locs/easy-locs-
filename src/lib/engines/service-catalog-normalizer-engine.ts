/** Stub — engine logic moved to backend functions. */
export async function runServiceCatalogNormalizer(..._args: any[]): Promise<any> {
  console.warn("[service-catalog-normalizer-engine] Stub — no-op");
  return { status: "stub", results: [] };
}
