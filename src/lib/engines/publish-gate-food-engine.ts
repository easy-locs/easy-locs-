/** Stub — engine logic moved to backend functions. */
export async function runPublishGateFood(..._args: any[]): Promise<any> {
  console.warn("[publish-gate-food-engine] Stub — no-op");
  return { status: "stub", results: [] };
}
