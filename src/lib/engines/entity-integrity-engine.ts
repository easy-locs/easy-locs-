/** Stub — engine logic moved to backend functions. */
export async function runEntityIntegrity(..._args: any[]): Promise<any> {
  console.warn("[entity-integrity-engine] Stub — no-op");
  return { status: "stub", results: [] };
}
