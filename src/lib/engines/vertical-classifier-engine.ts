/** Stub — engine logic moved to backend functions. */
export async function runVerticalClassifier(..._args: any[]): Promise<any> {
  console.warn("[vertical-classifier-engine] Stub — no-op");
  return { status: "stub", results: [] };
}
