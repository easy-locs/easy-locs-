/** Stub — engine logic moved to backend functions. */
export async function runStrictQualityGate(..._args: any[]): Promise<any> {
  console.warn("[strict-quality-gate-engine] Stub — no-op");
  return { status: "stub", results: [] };
}
