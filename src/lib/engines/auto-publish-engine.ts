/** Stub — engine logic moved to backend functions. */
export async function runAutoPublish(..._args: any[]): Promise<any> {
  console.warn("[auto-publish-engine] Stub — no-op");
  return { status: "stub", results: [] };
}
