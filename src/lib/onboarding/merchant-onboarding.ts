/**
 * merchant-onboarding — Stub.
 */
export async function startMerchantOnboarding(_userId: string) {
  return { step: "info", status: "pending" };
}

export async function activateMerchantProfile(..._args: any[]) {
  return { success: true };
}
