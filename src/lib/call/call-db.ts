/**
 * Call DB operations — re-exports from canonical repository.
 * Kept for backward compatibility with CallManager.
 */
export { markCallActive, markCallDeclined, markCallEnded } from "@/repositories/communication.repository";
