# ╔══════════════════════════════════════════════════════════════════════════════╗
# ║  LEGACY_INACTIVE — IONOS Deploy Now — NOT EXECUTED BY GITHUB ACTIONS       ║
# ║                                                                              ║
# ║  DEPLOYMENT TARGET: IONOS Deploy Now (NOT the canonical deployment target)  ║
# ║  CANONICAL TARGET:  Vercel (see .github/workflows/deploy-vercel.yml)        ║
# ║                                                                              ║
# ║  This file lives at easy-locs-ea1eb0ed/.github/workflows/ — a SUBDIRECTORY  ║
# ║  of the repo root. GitHub Actions NEVER reads files here. This workflow     ║
# ║  has NEVER been executed since it was placed in this location.              ║
# ║                                                                              ║
# ║  IONOS Deploy Now is a LEGACY/FALLBACK target. It is NOT:                   ║
# ║    - the production deployment target                                        ║
# ║    - the preview deployment target                                           ║
# ║    - an actively maintained deployment path                                  ║
# ║                                                                              ║
# ║  To activate IONOS deployment you would need to:                            ║
# ║    1. Move this file to .github/workflows/deploy-ionos.yml (repo root)      ║
# ║    2. Set IONOS_PROJECT_ID, IONOS_BRANCH_ID, IONOS_API_KEY secrets          ║
# ║    3. Explicitly declare IONOS canonical in CANONICAL_DEPLOYMENT.md         ║
# ╚══════════════════════════════════════════════════════════════════════════════╝

name: Deploy to IONOS

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Enforce pillar route ownership
        run: npm run check:pillar-routes

      - name: Build project
        run: npm run build
        env:
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          VITE_SUPABASE_PUBLISHABLE_KEY: ${{ secrets.VITE_SUPABASE_PUBLISHABLE_KEY }}

      - name: Deploy to IONOS
        uses: ionos-deploy-now/deploy-to-ionos@v1
        with:
          project-id: ${{ secrets.IONOS_PROJECT_ID }}
          branch-id: ${{ secrets.IONOS_BRANCH_ID }}
          service-host: api-us.ionos.space
          api-key: ${{ secrets.IONOS_API_KEY }}
          dist-folder: dist
