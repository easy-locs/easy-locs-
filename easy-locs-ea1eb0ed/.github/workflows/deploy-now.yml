# DEPRECATED: This workflow has been replaced by the Vercel-based deployment pipeline.
# See /.github/workflows/vercel-production.yml for production deployments
# and /.github/workflows/vercel-preview.yml for preview deployments.
#
# This file is kept for reference only. Remove after confirming Vercel pipeline
# is fully operational and IONOS secrets have been cleaned up.

name: "[DEPRECATED] Deploy to IONOS"

on:
  workflow_dispatch:

jobs:
  deprecated-notice:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecation Notice
        run: |
          echo "::warning::This workflow is deprecated. Use the Vercel deployment pipeline instead."
          echo "Production deployments now happen automatically via Vercel on merge to main."
          echo "Preview deployments are created automatically for every PR."
          exit 1
