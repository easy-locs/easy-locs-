name = "easy-locs"
compatibility_date = "2024-09-23"
pages_build_output_dir = "dist"

# Cloudflare Pages configuration.
# Deploy command (manual): wrangler pages deploy dist --project-name easy-locs
# NEVER use: wrangler versions upload --yes (invalid for Pages)
