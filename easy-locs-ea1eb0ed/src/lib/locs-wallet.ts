/**
 * locs-wallet — LOCS credits formatting helpers.
 * Re-exports the canonical formatter from orbit-payments so all
 * UI code can import from a single short alias.
 */
export { formatLocs } from "@/lib/orbit-payments/currency-detect";
