/**
 * ride-rating.handler — DEPRECATED: merged into close-flow-engine.ts
 * Rating requests are now part of the unified close flow.
 */
import { eventBus } from "@/lib/core/event-bus";

export function initRideRatingHandler() {
  // Handled by close-flow-engine — no duplicate emit
}
