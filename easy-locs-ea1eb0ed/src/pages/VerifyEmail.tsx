/**
 * VerifyEmail — legacy route kept for back-compat with transactional email links.
 * The unified VerifyAccount page handles both email and phone verification flows.
 */
export { default } from "./VerifyAccount";
