import SectionPlaceholder from "./SectionPlaceholder";
import { getSection } from "../sections";

export default function EnginesSection() {
  return <SectionPlaceholder section={getSection("engines")} />;
}
