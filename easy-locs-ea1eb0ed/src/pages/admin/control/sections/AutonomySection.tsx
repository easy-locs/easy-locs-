import SectionPlaceholder from "./SectionPlaceholder";
import { getSection } from "../sections";

export default function AutonomySection() {
  return <SectionPlaceholder section={getSection("autonomy")} />;
}
